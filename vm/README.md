# VM / ISO (out of scope for fatia-1)

Trace Labs / custom Kali ISO imaging is **deferred**.

Fatia-1 uses the `kali` Docker Compose profile (`docker compose --profile kali up --build`) as a lightweight sidecar.

Future fatias may add:
- Packer/ISO build notes
- Persistent lab VM with Evidence locker
- Offline tool mirrors
